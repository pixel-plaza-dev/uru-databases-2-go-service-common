{
  "$jsonSchema": {
    "required": [
      "user_id",
      "uuid"
    ],
    "properties": {
      "user_id": {
        "bsonType": "objectId",
        "description": "must be an objectId and is required"
      },
      "uuid": {
        "bsonType": "string",
        "description": "must be a string and is required",
        "minLength": 36,
        "maxLength": 36
      }
    }
  }
}