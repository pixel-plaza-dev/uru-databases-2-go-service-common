package utils

// AddBrackets adds brackets to a string
func AddBrackets(name string) string {
	return "[" + name + "]"
}

// AddParentheses adds parentheses to a string
func AddParentheses(name string) string {
	return "(" + name + ")"
}
